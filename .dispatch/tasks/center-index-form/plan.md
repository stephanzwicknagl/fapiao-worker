# Center Form on Index Page

- [x] Read `templates/index.html` and understand the current layout (mvp.css is the base stylesheet), then add CSS to center the main content both vertically and horizontally on the page — added `body { min-height: 100vh; display: flex; flex-direction: column; justify-content: center; }` which centers vertically; horizontal centering is already handled by mvp.css's max-width on `main`
